apiVersion: v1
kind: Service
metadata:
  name: customer-frontend
  namespace: tracking-app
spec:
  type: NodePort
  selector:
    app: customer-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008
