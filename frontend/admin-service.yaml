apiVersion: v1
kind: Service
metadata:
  name: admin-frontend
  namespace: tracking-app
spec:
  type: NodePort
  selector:
    app: admin-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007
