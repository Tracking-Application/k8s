apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: tracking-app
spec:
  type: ClusterIP
  selector:
    app: tracking-service
  ports:
    - name: http
      port: 8004
      targetPort: 8004
      protocol: TCP
